import { Router } from 'express';
// import authRoutes from './auth.route';
// import profileRoutes from './profile.route';

const router = Router();

// // Group routes under /api/patient
// router.use('/', authRoutes);
// router.use('/', profileRoutes);

export default router;
